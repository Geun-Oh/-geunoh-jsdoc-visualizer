/**
 * 두 string 값을 받아서 hello + a + b를 반환합니다.
 * @param {string} a 먼저 오는 string
 * @param {string} b 다음에 오는 string
 * @param {string} c 새로운 c 입니다.
 * @returns  hello + a + b 를 반환합니다.
 */
const func = (a, b, c) => {
  return "hello" + a + b;
};
